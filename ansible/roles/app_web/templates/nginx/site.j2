server {
	listen 80;
	listen [::]:80;

	server_name {{ app_server_name }};

	# Serve the static files directly.
	location ~ ^/(js/|css/|images/) {
		root {{ app_deploy_path }}/app/public;
		access_log off;
		expires max;
	}

	location / {
		proxy_pass http://127.0.0.1:3000;
	}
}