; inventories/vagrant
; Vagrant setup with a single server

; server
localdev ansible_host=192.168.33.10 ansible_user=vagrant ansible_private_key_file=.vagrant/machines/default/virtualbox/private_key

[web]
localdev

[db]
localdev

[vagrant:children]
web
db